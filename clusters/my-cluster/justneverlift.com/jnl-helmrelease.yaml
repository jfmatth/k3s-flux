apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: justneverlift
  namespace: justneverlift
spec:
  interval: 10m
  # chart:
  #   spec:
  #     chart: ./_helm
  #     version: '0.1.0'
  #     sourceRef:
  #       kind: GitRepository
  #       name: justneverlift
  #       namespace: flux-system
  #     interval: 10m
  chartRef:
    kind: HelmChart
    name: justneverlift
    namespace: flux-system
  values:
    image:
      repository: ghcr.io/jfmatth/justneverlift.com  # {"$imagepolicy": "flux-system:justneverlift:name"}
      tag: v2025.11.20.0                             # {"$imagepolicy": "flux-system:justneverlift:tag"}
    replicaCount: 2
